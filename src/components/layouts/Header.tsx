// packages
import React from "react";

// components
import Nav from "../global/Nav";

export default function Header() {
  return (
    <header>
      <Nav />
      <p>header</p>
    </header>
  );
}
